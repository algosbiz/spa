// TEMPORARY preview page for contact design 4 (Atlas — map inside the section).
// Delete together with the other contact-design-* pages and
// components/sections/contact/_PreviewShell.js once a design is chosen.
import PreviewShell from '../components/sections/contact/_PreviewShell';

export default function ContactDesign4() {
    return <PreviewShell design="atlas" />;
}
